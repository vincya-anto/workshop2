/** Keep in sync with `@media (min-width: 1024px)` / `(max-width: 1023px)` in `globals.css`. */
export const PUSH_LAYOUT_MIN_PX = 1024

/** Navbar condensed + hide `slot="center"` below this width (`md`). */
export const NAVBAR_WIDE_MIN_PX = 768

/** Default expanded rail from this width (`xl`). */
export const XL_MIN_PX = 1280

export const SIDE_NAV_MIN_WIDTH = '4rem'
export const SIDE_NAV_MAX_WIDTH = '256px'

export const APP_SHELL_ROOT_CLASS = 'app-shell'
export const APP_SHELL_RAIL_EXPANDED_CLASS = 'app-shell--rail-expanded'
